import React from 'react';
import GroundCard from './GroundCard';

function GroundList() {
  return (
    <div>
      <GroundCard />
      <GroundCard />
      <GroundCard />
    </div>
  );
}

export default GroundList;
